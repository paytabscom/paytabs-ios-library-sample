# Use git specs repo to avoid CDN HTTP/2 framing errors (e.g. "Error in the HTTP2 framing layer")
source 'https://github.com/CocoaPods/Specs.git'

# Define a global platform for your project
platform :ios, '15.0'

target 'sample-swiftui' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Use local pod from repo root to avoid CDN; remove :path to use published version from trunk
  pod 'PayTabsSDK'
end
